function check_all(sort_condition) {
  if($("." + sort_condition + "_check_music").not(":checked").length > 0) {
    $("." + sort_condition + "_check_music").prop('checked', true);
  } else {
    $("." + sort_condition + "_check_music").prop('checked', false);
  }
}
